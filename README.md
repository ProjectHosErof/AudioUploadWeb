# AudioUploadWeb

A landing page designed to help collect hymn audio files by the community's support for more efficient development.

# Frontend Setup

This frontend is built using React + Vite + TypeScript.

## Start the dev server

npm install
npm run dev
